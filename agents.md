# Agent Notes

- This repository contains graduate macroeconomics lecture materials, mainly Quarto (`.qmd`), LaTeX (`.tex`), and generated outputs (`.html`, `.pdf`).
- Prefer editing source files (`.qmd`, `.tex`, scripts) rather than generated `.html` or `.pdf` files unless explicitly asked.
- Keep Japanese lecture prose natural and concise; preserve mathematical notation and economic terminology carefully.
- Avoid broad rewrites, unrelated formatting churn, and changes to generated asset directories such as `*_files/`.
- When changing lecture sources, verify syntax where practical with the relevant Quarto or LaTeX build command.
- Do not overwrite existing user changes. Check the working tree before committing or large edits.
